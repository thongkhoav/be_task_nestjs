import { User } from './entities/user.entity';
import { DataSource, EntityManager, Repository } from 'typeorm';
import { AuthDto } from './dto';
import { Tokens } from './types';
import { ConfigService } from '@nestjs/config';
import { JwtService } from '@nestjs/jwt';
import { LoginRequestDto } from './dto/login-request.dto';
import { Role } from './entities/role.entity';
import { LoginSession } from './entities/login-session.entity';
export declare class AuthService {
    private userRepo;
    private roleRepo;
    private loginSessionRepository;
    private jwtService;
    private config;
    private dataSource;
    private readonly entityManager;
    constructor(userRepo: Repository<User>, roleRepo: Repository<Role>, loginSessionRepository: Repository<LoginSession>, jwtService: JwtService, config: ConfigService, dataSource: DataSource, entityManager: EntityManager);
    isExistEmail(email: string): Promise<boolean>;
    validateUser(email: string, pass: string): Promise<any>;
    register(dto: AuthDto): Promise<void>;
    login(loginRequestDto: LoginRequestDto): Promise<Tokens>;
    logout(userId: string, refreshToken: string, accessToken: string): Promise<void>;
    getUserFromToken(token: string): Promise<User | null>;
    getUserById(id: string): Promise<User | null>;
    private createNewRefreshToken;
    refreshAccessToken(tokenDto: Tokens): Promise<Tokens>;
    validRefreshToken(email: string, refreshToken: string): Promise<any>;
    private createAccessToken;
}
